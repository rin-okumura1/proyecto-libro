/*const assert =require("chai")
const app=require("../app.js")
const request = require('supertest');

describe("test users",function(){

it("post de users",function(done){ 

    request(app)
     .post('/users')
     .send({ "name":"nelson",
     "surname":"pacheco",
     "email" : "lalala@correo.com",
     "password":123554,
     "score":1,
     "statusId":2})
     .expect(400,done)})
    
    })*/

